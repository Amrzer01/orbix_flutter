name: orbix_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^11.0.0
  google_fonts: ^6.2.1
  flutter_card_swiper: ^7.2.0
  cached_network_image: ^3.4.1
  shared_preferences: ^2.3.3
  provider: ^6.1.2
  http: ^1.2.2
  url_launcher: ^6.3.1

  # ===== Firebase (إصدارات مستقرة ومتوافقة) =====
  firebase_core: ^3.8.1
  cloud_firestore: ^5.6.0
  firebase_auth: ^5.3.4
  firebase_messaging: ^15.1.6
  firebase_storage: ^12.3.7

  flutter_local_notifications: ^18.0.1
  image_picker: ^1.1.2
  nfc_manager: ^3.5.0
  qr_flutter: ^4.1.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true